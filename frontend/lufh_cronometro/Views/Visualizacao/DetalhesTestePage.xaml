<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp.Views.Visualizacao.DetalhesTestePage"
             Title="Detalhes do Teste"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Header -->
            <Frame Padding="20" 
                   CornerRadius="10" 
                   BackgroundColor="White"
                   HasShadow="True">
                <VerticalStackLayout Spacing="8">
                    <Label Text="🧪 Detalhes do Teste"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="#1976D2"/>
                    <Label x:Name="NomeLabel"
                           Text="-"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="#333"/>
                    <HorizontalStackLayout Spacing="10">
                        <Frame Padding="8,4"
                               CornerRadius="4"
                               BackgroundColor="#E8F5E9"
                               HasShadow="False">
                            <Label x:Name="StatusLabel"
                                   Text="ATIVO"
                                   FontSize="11"
                                   FontAttributes="Bold"
                                   TextColor="#4CAF50"/>
                        </Frame>
                        <Label x:Name="IdLabel"
                               Text="ID: -"
                               FontSize="12"
                               TextColor="#999"
                               VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>

            <!-- Informações Básicas -->
            <Frame Padding="20" 
                   CornerRadius="10" 
                   BackgroundColor="White"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    
                    <Label Text="📋 Informações Básicas"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#333"/>

                    <!-- Objetivo -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Objetivo do Teste"
                               FontSize="12"
                               TextColor="#666"/>
                        <Label x:Name="ObjetivoLabel"
                               Text="-"
                               FontSize="14"
                               TextColor="#333"/>
                    </VerticalStackLayout>

                    <BoxView HeightRequest="1" BackgroundColor="#E0E0E0"/>

                    <!-- Data de Criação -->
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                        <Label Grid.Column="0"
                               Text="📅"
                               FontSize="20"
                               VerticalOptions="Center"/>
                        <VerticalStackLayout Grid.Column="1" Spacing="3">
                            <Label Text="Data de Criação"
                                   FontSize="12"
                                   TextColor="#666"/>
                            <Label x:Name="DataCriacaoLabel"
                                   Text="-"
                                   FontSize="14"
                                   TextColor="#333"/>
                        </VerticalStackLayout>
                    </Grid>

                </VerticalStackLayout>
            </Frame>

            <!-- Relacionamentos -->
            <Frame Padding="20" 
                   CornerRadius="10" 
                   BackgroundColor="White"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    
                    <Label Text="🔗 Relacionamentos"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#333"/>

                    <!-- Cliente -->
                    <Frame Padding="15"
                           CornerRadius="8"
                           BackgroundColor="#E3F2FD"
                           HasShadow="False">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                            <Label Grid.Column="0"
                                   Text="👤"
                                   FontSize="24"
                                   VerticalOptions="Center"/>
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="3">
                                <Label Text="Cliente"
                                       FontSize="12"
                                       TextColor="#1976D2"/>
                                <Label x:Name="ClienteLabel"
                                       Text="-"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="#333"/>
                            </VerticalStackLayout>
                            <Label Grid.Column="2"
                                   Text="›"
                                   FontSize="24"
                                   TextColor="#1976D2"
                                   VerticalOptions="Center"/>
                        </Grid>
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnClienteClicked"/>
                        </Frame.GestureRecognizers>
                    </Frame>

                    <!-- Produto -->
                    <!-- Remover ou comentar esta seção: -->
                    <!--
                    <Frame Padding="15"
                           CornerRadius="8"
                           BackgroundColor="#F3E5F5"
                           HasShadow="False">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                            <Label Grid.Column="0"
                                   Text="📦"
                                   FontSize="24"
                                   VerticalOptions="Center"/>
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="3">
                                <Label Text="Produto"
                                       FontSize="12"
                                       TextColor="#9C27B0"/>
                                <Label x:Name="ProdutoLabel"
                                       Text="-"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="#333"/>
                            </VerticalStackLayout>
                            <Label Grid.Column="2"
                                   Text="›"
                                   FontSize="24"
                                   TextColor="#9C27B0"
                                   VerticalOptions="Center"/>
                        </Grid>
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnProdutoClicked"/>
                        </Frame.GestureRecognizers>
                    </Frame>
                    -->

                    <!-- Coordenador -->
                    <Frame Padding="15"
                           CornerRadius="8"
                           BackgroundColor="#FFF3E0"
                           HasShadow="False">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                            <Label Grid.Column="0"
                                   Text="👥"
                                   FontSize="24"
                                   VerticalOptions="Center"/>
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="3">
                                <Label Text="Coordenador"
                                       FontSize="12"
                                       TextColor="#FF9800"/>
                                <Label x:Name="CoordenadorLabel"
                                       Text="-"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="#333"/>
                            </VerticalStackLayout>
                            <Label Grid.Column="2"
                                   Text="›"
                                   FontSize="24"
                                   TextColor="#FF9800"
                                   VerticalOptions="Center"/>
                        </Grid>
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCoordenadorClicked"/>
                        </Frame.GestureRecognizers>
                    </Frame>

                </VerticalStackLayout>
            </Frame>

            <!-- Estatísticas de Mensurações -->
            <Frame Padding="20" 
                   CornerRadius="10" 
                   BackgroundColor="White"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    
                    <Label Text="📊 Estatísticas de Mensurações"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#333"/>

                    <Grid ColumnDefinitions="*,*,*" ColumnSpacing="10">
                        
                        <!-- Total -->
                        <Frame Grid.Column="0"
                               Padding="15"
                               CornerRadius="8"
                               BackgroundColor="#E3F2FD">
                            <VerticalStackLayout Spacing="5">
                                <Label Text="📝"
                                       FontSize="24"
                                       HorizontalOptions="Center"/>
                                <Label x:Name="TotalMensuracoesLabel"
                                       Text="0"
                                       FontSize="28"
                                       FontAttributes="Bold"
                                       TextColor="#1976D2"
                                       HorizontalOptions="Center"/>
                                <Label Text="Total"
                                       FontSize="11"
                                       TextColor="#666"
                                       HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                        </Frame>

                        <!-- Concluídas -->
                        <Frame Grid.Column="1"
                               Padding="15"
                               CornerRadius="8"
                               BackgroundColor="#E8F5E9">
                            <VerticalStackLayout Spacing="5">
                                <Label Text="✅"
                                       FontSize="24"
                                       HorizontalOptions="Center"/>
                                <Label x:Name="ConcluidasLabel"
                                       Text="0"
                                       FontSize="28"
                                       FontAttributes="Bold"
                                       TextColor="#4CAF50"
                                       HorizontalOptions="Center"/>
                                <Label Text="Concluídas"
                                       FontSize="11"
                                       TextColor="#666"
                                       HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                        </Frame>

                        <!-- Em Andamento -->
                        <Frame Grid.Column="2"
                               Padding="15"
                               CornerRadius="8"
                               BackgroundColor="#FFF3E0">
                            <VerticalStackLayout Spacing="5">
                                <Label Text="⏳"
                                       FontSize="24"
                                       HorizontalOptions="Center"/>
                                <Label x:Name="EmAndamentoLabel"
                                       Text="0"
                                       FontSize="28"
                                       FontAttributes="Bold"
                                       TextColor="#FF9800"
                                       HorizontalOptions="Center"/>
                                <Label Text="Andamento"
                                       FontSize="11"
                                       TextColor="#666"
                                       HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                        </Frame>

                    </Grid>

                    <!-- Botão Ver Mensurações -->
                    <Button Text="VER MENSURAÇÕES"
                            Clicked="OnVerMensuracoesClicked"
                            BackgroundColor="#2196F3"
                            TextColor="White"
                            CornerRadius="8"
                            HeightRequest="45"
                            Margin="0,10,0,0"/>

                </VerticalStackLayout>
            </Frame>

            <!-- Roteiros -->
            <Frame Padding="20" 
                   CornerRadius="10" 
                   BackgroundColor="White"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    
                    <Label Text="📝 Roteiros do Teste"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#333"/>

                    <Label x:Name="RoteirosLabel"
                           Text="Nenhum roteiro configurado."
                           FontSize="14"
                           TextColor="#666"/>

                    <Button Text="CONFIGURAR ROTEIROS"
                            Clicked="OnConfigurarRoteirosClicked"
                            BackgroundColor="#FF5722"
                            TextColor="White"
                            CornerRadius="8"
                            HeightRequest="45"/>

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>