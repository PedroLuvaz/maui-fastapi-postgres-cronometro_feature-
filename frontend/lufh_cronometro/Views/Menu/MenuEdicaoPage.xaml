<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp.Views.Menu.MenuEdicaoPage"
             Title="Editar"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Header -->
            <Frame Padding="20" 
                   CornerRadius="10" 
                   BackgroundColor="White"
                   HasShadow="True">
                <VerticalStackLayout Spacing="5">
                    <Label Text="✏️ Edição"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="#FF9800"/>
                    <Label Text="Selecione o que deseja editar"
                           FontSize="14"
                           TextColor="#666"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Lista de Opções -->
            <VerticalStackLayout Spacing="15">

                <!-- Cliente -->
                <Frame Padding="20"
                       CornerRadius="10"
                       HasShadow="True"
                       BackgroundColor="White">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="15">
                        <Label Grid.Column="0"
                               Text="👤"
                               FontSize="30"
                               VerticalOptions="Center"/>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="3">
                            <Label Text="Clientes"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#333"/>
                            <Label Text="Editar clientes cadastrados"
                                   FontSize="12"
                                   TextColor="#666"/>
                        </VerticalStackLayout>
                        <Label Grid.Column="2"
                               Text="›"
                               FontSize="30"
                               TextColor="#FF9800"
                               VerticalOptions="Center"/>
                    </Grid>
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnEditarClientesClicked"/>
                    </Frame.GestureRecognizers>
                </Frame>

                <!-- Produto -->
                <Frame Padding="20"
                       CornerRadius="10"
                       HasShadow="True"
                       BackgroundColor="White">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="15">
                        <Label Grid.Column="0"
                               Text="📦"
                               FontSize="30"
                               VerticalOptions="Center"/>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="3">
                            <Label Text="Produtos"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#333"/>
                            <Label Text="Editar produtos cadastrados"
                                   FontSize="12"
                                   TextColor="#666"/>
                        </VerticalStackLayout>
                        <Label Grid.Column="2"
                               Text="›"
                               FontSize="30"
                               TextColor="#FF9800"
                               VerticalOptions="Center"/>
                    </Grid>
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnEditarProdutosClicked"/>
                    </Frame.GestureRecognizers>
                </Frame>

                <!-- Usuário -->
                <Frame Padding="20"
                       CornerRadius="10"
                       HasShadow="True"
                       BackgroundColor="White">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="15">
                        <Label Grid.Column="0"
                               Text="👥"
                               FontSize="30"
                               VerticalOptions="Center"/>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="3">
                            <Label Text="Usuários"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#333"/>
                            <Label Text="Editar usuários do sistema"
                                   FontSize="12"
                                   TextColor="#666"/>
                        </VerticalStackLayout>
                        <Label Grid.Column="2"
                               Text="›"
                               FontSize="30"
                               TextColor="#FF9800"
                               VerticalOptions="Center"/>
                    </Grid>
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnEditarUsuariosClicked"/>
                    </Frame.GestureRecognizers>
                </Frame>

                <!-- Teste -->
                <Frame Padding="20"
                       CornerRadius="10"
                       HasShadow="True"
                       BackgroundColor="White">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="15">
                        <Label Grid.Column="0"
                               Text="🧪"
                               FontSize="30"
                               VerticalOptions="Center"/>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="3">
                            <Label Text="Testes"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#333"/>
                            <Label Text="Editar testes de usabilidade"
                                   FontSize="12"
                                   TextColor="#666"/>
                        </VerticalStackLayout>
                        <Label Grid.Column="2"
                               Text="›"
                               FontSize="30"
                               TextColor="#FF9800"
                               VerticalOptions="Center"/>
                    </Grid>
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnEditarTestesClicked"/>
                    </Frame.GestureRecognizers>
                </Frame>

            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>